{
  "name": "nifty50-banknifty-live",
  "version": "1.0.0",
  "description": "Minimal server to serve a frontend and optionally proxy live data",
  "main": "proxy.js",
  "scripts": {
    "start": "node proxy.js",
    "dev": "nodemon proxy.js",
    "build": "echo \"No build step configured\""
  },
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "http-proxy-middleware": "^2.0.6"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
